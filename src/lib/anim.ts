// Shared easing curve (exponential ease-out), typed as a cubic-bezier tuple
// so framer-motion accepts it under strict TypeScript.
export const EASE = [0.16, 1, 0.3, 1] as [number, number, number, number];
